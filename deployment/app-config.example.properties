# ============================================================
# Shree Annapurna Smart Billing System
# Database Configuration — EXAMPLE / TEMPLATE
# ============================================================
#
# Copy this file to: deployment/app-config.properties
# Fill in your actual values before running the application.
#
# DO NOT commit app-config.properties to version control —
# it is listed in .gitignore because it contains real credentials.
# ============================================================

db.host=localhost
db.port=33066
db.name=smart_billing_db
db.user=root
db.password=YOUR_PASSWORD_HERE
